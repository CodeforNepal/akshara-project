server {
    listen       8000;
    server_name  localhost;

    location / {
        proxy_pass http://localhost:8080;
    }

    location /api {
        proxy_http_version               1.1;
        proxy_pass http://sangraha.org/es;
        add_header Access-Control-Allow-Origin *;
    }
}
